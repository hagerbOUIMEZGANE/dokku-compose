plugins:
  postgres:
    url: https://github.com/dokku/dokku-postgres.git

networks:
  - app-net

services:
  myapp-postgres:
    plugin: postgres

apps:
  myapp:
    build:
      context: apps/myapp
    ports:
      - "http:5000:5000"
    links:
      - myapp-postgres
